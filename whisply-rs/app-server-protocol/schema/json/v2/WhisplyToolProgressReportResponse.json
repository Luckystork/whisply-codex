{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "Whether the report was attributable to a call that is actually running. A refused report is not an error the owner should retry; the call it named has already ended or never belonged to this connection.",
  "properties": {
    "published": {
      "type": "boolean"
    }
  },
  "required": [
    "published"
  ],
  "title": "WhisplyToolProgressReportResponse",
  "type": "object"
}