# The product runs its async work on 16 MiB threads (see arg0's
# TOKIO_WORKER_STACK_SIZE_BYTES), because these futures are far deeper in debug
# builds than the 2 MiB an OS thread gets by default. Test binaries spawn their
# threads through `std::thread`, which reads this, so tests exercise the same
# budget the product gives itself instead of aborting on a stack overflow.
[env]
RUST_MIN_STACK = { value = "16777216", force = false }

[target.'cfg(all(windows, target_env = "msvc"))']
rustflags = ["-C", "link-arg=/STACK:8388608", "-C", "target-feature=+crt-static"]

# MSVC emits a warning about code that may trip "Cortex-A53 MPCore processor bug #843419" (see
# https://developer.arm.com/documentation/epm048406/latest) which is sometimes emitted by LLVM.
# Since Arm64 Windows 10+ isn't supported on that processor, it's safe to disable the warning.
[target.aarch64-pc-windows-msvc]
rustflags = ["-C", "link-arg=/STACK:8388608", "-C", "link-arg=/arm64hazardfree"]

[target.'cfg(all(windows, target_env = "gnu"))']
rustflags = ["-C", "link-arg=-Wl,--stack,8388608"]
